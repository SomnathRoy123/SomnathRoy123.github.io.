---
layout: default
---

<style>
  /* 1. Typography & Global Settings */
  .cv-page {
    font-family: 'Lato', 'Helvetica Neue', Helvetica, Arial, sans-serif;
    color: #333;
    max-width: 1000px;
    margin: 0 auto;
    background: #fff;
  }

  h1, h2, h3, h4 {
    margin: 0;
    padding: 0;
  }

  a { color: #2b2b2b; text-decoration: none; }
  a:hover { color: #0056b3; text-decoration: underline; }

  /* 2. The Header (Name & Links) */
  .cv-header {
    text-align: center;
    margin-bottom: 40px;
    border-bottom: 1px solid #eee;
    padding-bottom: 20px;
  }

  .cv-name {
    font-family: "Georgia", serif; /* Academic Touch */
    font-size: 2.8rem;
    font-weight: 700;
    letter-spacing: -0.5px;
    color: #111;
    text-transform: uppercase;
  }

  .cv-subtitle {
    font-family: 'Lato', sans-serif;
    font-size: 1rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: #666;
    margin-top: 5px;
    margin-bottom: 15px;
  }

  .cv-contact-info {
    font-size: 0.9rem;
    color: #555;
  }

  .cv-contact-info span { margin: 0 8px; }

  /* 3. The Two-Column Grid */
  .cv-grid {
    display: grid;
    grid-template-columns: 32% 64%; /* Left Narrow, Right Wide */
    gap: 4%;
  }

  @media (max-width: 768px) {
    .cv-grid { grid-template-columns: 1fr; }
  }

  /* 4. Section Headers */
  .section-title {
    font-family: 'Lato', sans-serif;
    font-weight: 900;
    font-size: 1.1rem;
    color: #222;
    text-transform: uppercase;
    letter-spacing: 1px;
    border-bottom: 2px solid #ddd;
    padding-bottom: 5px;
    margin-top: 0px;
    margin-bottom: 15px;
  }

  .right-col .section-title {
    margin-top: 0px; /* Reset top margin for right col alignment */
  }

  /* 5. Left Column Styling (Education, Skills) */
  .edu-block { margin-bottom: 25px; }
  
  .edu-school {
    font-weight: 700;
    font-size: 1rem;
    color: #000;
  }
  
  .edu-degree {
    font-size: 0.95rem;
    font-style: italic;
    color: #444;
    margin-bottom: 2px;
  }
  
  .edu-date {
    font-size: 0.85rem;
    color: #777;
    margin-bottom: 8px;
  }

  .edu-score {
    font-size: 0.9rem;
    font-weight: 600;
    color: #333;
  }

  .skill-block { margin-bottom: 20px; }
  .skill-cat { font-weight: 700; font-size: 0.9rem; color: #000; margin-bottom: 4px; }
  .skill-list { font-size: 0.9rem; color: #444; line-height: 1.4; }

  /* 6. Right Column Styling (Experience, Research) */
  .exp-block { margin-bottom: 30px; }

  .exp-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 5px;
  }

  .exp-role {
    font-family: 'Lato', sans-serif;
    font-weight: 700;
    font-size: 1.1rem;
    color: #000;
  }

  .exp-company {
    font-family: "Georgia", serif;
    font-style: italic;
    font-size: 1rem;
    color: #333;
  }

  .exp-date {
    font-size: 0.85rem;
    font-weight: 600;
    color: #777;
    text-align: right;
    min-width: 100px;
  }

  .exp-summary {
    font-size: 0.95rem;
    line-height: 1.5;
    color: #444;
    margin-bottom: 8px;
  }

  .exp-bullets ul {
    margin: 0;
    padding-left: 18px;
  }

  .exp-bullets li {
    font-size: 0.92rem;
    color: #444;
    margin-bottom: 4px;
    line-height: 1.45;
  }

</style>

<div class="post">
  
  <div style="text-align: right; margin-bottom: 10px;">
    <a href="{{ 'assets/pdf/cv.pdf' | relative_url }}" target="_blank" class="btn btn-sm btn-outline-dark">
      <i class="fas fa-file-pdf"></i> PDF
    </a>
  </div>

  <article class="cv-page">

    <header class="cv-header">
      <div class="cv-name">{{ site.data.cv.name }}</div>
      <div class="cv-subtitle">{{ site.data.cv.label }}</div>
      <div class="cv-contact-info">
        <a href="mailto:{{ site.data.cv.email }}">{{ site.data.cv.email }}</a> <span>•</span>
        {{ site.data.cv.address.city }}, {{ site.data.cv.address.countryCode }} <span>•</span>
        {% for social in site.data.cv.social_networks %}
          <a href="{{ social.url }}">{{ social.network }}</a> {% unless forloop.last %}<span>•</span>{% endunless %}
        {% endfor %}
      </div>
    </header>

    <div class="cv-grid">
      
      <div class="left-col">
        
        <div class="section-title">Education</div>
        {% for entry in site.data.cv.sections.Education %}
        <div class="edu-block">
          <div class="edu-school">{{ entry.institution }}</div>
          <div class="edu-degree">{{ entry.studyType }}</div>
          <div class="edu-degree">{{ entry.area }}</div>
          <div class="edu-date">{{ entry.start_date }} – {{ entry.end_date }}</div>
          {% if entry.score %}
          <div class="edu-score">GPA: {{ entry.score }}</div>
          {% endif %}
          {% if entry.courses %}
          <div style="font-size: 0.85rem; color: #666; margin-top: 5px;">
            <em>{{ entry.courses }}</em>
          </div>
          {% endif %}
        </div>
        {% endfor %}

        <div class="section-title" style="margin-top: 30px;">Skills</div>
        {% for skill in site.data.cv.skills %}
        <div class="skill-block">
          <div class="skill-cat">{{ skill.name }}</div>
          <div class="skill-list">{{ skill.keywords }}</div>
        </div>
        {% endfor %}

        <div class="section-title" style="margin-top: 30px;">Awards</div>
        {% for entry in site.data.cv.sections.Awards %}
        <div class="mb-3">
          <div style="font-weight: 700; font-size: 0.9rem;">{{ entry.title }}</div>
          <div style="font-size: 0.85rem; font-style: italic;">{{ entry.awarder }}</div>
          <div style="font-size: 0.8rem; color: #777;">{{ entry.date }}</div>
        </div>
        {% endfor %}

      </div>

      <div class="right-col">
        
        <div class="section-title">Research Experience</div>
        {% for entry in site.data.cv.sections.Experience %}
        <div class="exp-block">
          <div class="exp-header">
            <div>
              <span class="exp-role">{{ entry.position }}</span>
            </div>
            <div class="exp-date">
              {{ entry.start_date | date: "%b %Y" }} – 
              {% if entry.end_date %}{{ entry.end_date | date: "%b %Y" }}{% else %}Present{% endif %}
            </div>
          </div>
          <div class="exp-company">{{ entry.company }} | {{ entry.location }}</div>
          
          <div class="exp-summary mt-2">{{ entry.summary }}</div>
          
          {% if entry.highlights %}
          <div class="exp-bullets">
            <ul>
              {% for item in entry.highlights %}
              <li>{{ item }}</li>
              {% endfor %}
            </ul>
          </div>
          {% endif %}
        </div>
        {% endfor %}

        {% if site.data.cv.sections.Publications %}
        <div class="section-title" style="margin-top: 40px;">Selected Publications</div>
        {% for entry in site.data.cv.sections.Publications %}
        <div class="mb-3">
          <div style="font-weight: 700; font-size: 1rem;">{{ entry.title }}</div>
          <div style="font-size: 0.95rem; font-style: italic;">{{ entry.authors | join: ", " }}</div>
          <div style="font-size: 0.9rem; color: #555;">{{ entry.publisher }} ({{ entry.releaseDate }})</div>
          <div style="font-size: 0.9rem; color: #444; margin-top: 2px;">{{ entry.summary }}</div>
        </div>
        {% endfor %}
        {% endif %}

      </div>
    </div>

  </article>
</div>